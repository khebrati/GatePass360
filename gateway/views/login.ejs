<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ورود | GatePass360</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .auth-page {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #f8f9fa;
            padding: 20px;
            margin-top: 60px;
        }
        
        .auth-card {
            background: white;
            border-radius: 10px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.1);
            width: 100%;
            max-width: 450px;
            padding: 40px;
        }
        
        .auth-header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .auth-header i {
            font-size: 3rem;
            color: #3498db;
            margin-bottom: 15px;
        }
        
        .auth-header h2 {
            color: #2c3e50;
            margin-bottom: 10px;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: #2c3e50;
        }
        
        .form-control {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid #ddd;
            border-radius: 5px;
            font-size: 1rem;
            font-family: inherit;
            transition: border 0.3s;
        }
        
        .form-control:focus {
            outline: none;
            border-color: #3498db;
        }
        
        .form-footer {
            text-align: center;
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid #eee;
        }
        
        .form-footer a {
            color: #3498db;
            text-decoration: none;
        }
        
        .error-message {
            background: #ffeaea;
            color: #d63031;
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 20px;
            display: none;
        }
        
        .forgot-password {
            display: block;
            text-align: left;
            margin-top: 5px;
            font-size: 0.9rem;
            color: #666;
            text-decoration: none;
        }
        
        .forgot-password:hover {
            color: #3498db;
        }
    </style>
</head>
<body>
    <!-- نوار بالا -->
    <nav class="navbar">
        <div class="container">
            <div class="logo">
                <i class="fas fa-door-closed"></i>
                <a href="/" style="text-decoration: none; color: inherit;">
                    GatePass<span class="highlight">360</span>
                </a>
            </div>
            <div class="nav-links">
                <a href="/register" class="btn-outline">ثبت‌نام</a>
                <a href="/" class="btn-primary">خانه</a>
            </div>
        </div>
    </nav>

    <!-- صفحه ورود -->
    <div class="auth-page">
        <div class="auth-card">
            <!-- هدر -->
            <div class="auth-header">
                <i class="fas fa-sign-in-alt"></i>
                <h2>ورود به سیستم</h2>
                <p>با حساب کاربری خود وارد شوید</p>
            </div>
            
            <!-- پیام خطا -->
            <div class="error-message" id="errorMessage"></div>
            
            <!-- فرم -->
            <form id="loginForm">
                <!-- ایمیل -->
                <div class="form-group">
                    <label for="email">ایمیل</label>
                    <input type="email" id="email" class="form-control" placeholder="example@domain.com" required>
                </div>
                
                <!-- رمز عبور -->
                <div class="form-group">
                    <label for="password">رمز عبور</label>
                    <input type="password" id="password" class="form-control" placeholder="رمز عبور خود را وارد کنید" required>
                    <a href="#" class="forgot-password">رمز عبور را فراموش کرده‌اید؟</a>
                </div>
                
                <!-- دکمه ورود -->
                <button type="submit" class="btn-primary" style="width: 100%; padding: 12px;">
                    <i class="fas fa-sign-in-alt"></i> ورود
                </button>
                
                <!-- فوتر فرم -->
                <div class="form-footer">
                    <p>حساب کاربری ندارید؟ <a href="/register">ثبت‌نام کنید</a></p>
                    <a href="/" style="color: #666; text-decoration: none;">
                        <i class="fas fa-arrow-right"></i> بازگشت به خانه
                    </a>
                </div>
            </form>
        </div>
    </div>

    <!-- 🔴 اسکریپت اصلاح شده -->
    <script>
        // تابع نمایش خطا
        function showError(message) {
            const errorDiv = document.getElementById('errorMessage');
            errorDiv.textContent = message;
            errorDiv.style.display = 'block';
        }
        
        // فرم ورود
        const form = document.getElementById('loginForm');
        
        form.addEventListener('submit', function(e) {
            e.preventDefault();
            
            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;
            
            // پاک کردن خطاهای قبلی
            document.getElementById('errorMessage').style.display = 'none';
            
            // 🔴 شبیه‌سازی ورود - کاربران نمونه
            const users = {
                'guest@example.com': {password: '123456', role: 'guest', name: 'مهمان نمونه'},
                'host@example.com': {password: '123456', role: 'host', name: 'میزبان نمونه'},
                'security@example.com': {password: '123456', role: 'security', name: 'حراست نمونه'},
                'admin@example.com': {password: '123456', role: 'admin', name: 'مدیر نمونه'}
            };
            
            if (users[email] && users[email].password === password) {
                const user = users[email];
                
                // ذخیره در localStorage
                localStorage.setItem('user', JSON.stringify({
                    email: email,
                    name: user.name,
                    role: user.role,
                    loggedIn: true
                }));
                
                // 🔴 هدایت بر اساس نقش
                const redirects = {
                    'guest': '/panel/guest',
                    'host': '/panel/host',
                    'security': '/panel/security',
                    'admin': '/panel/admin'
                };
                
                window.location.href = redirects[user.role];
                
            } else {
                showError('ایمیل یا رمز عبور اشتباه است');
            }
        });
        
        // 🔴 اگر کاربر قبلاً وارد شده، بررسی کن و هدایت کن
        document.addEventListener('DOMContentLoaded', function() {
            const user = localStorage.getItem('user');
            if (user) {
                const userObj = JSON.parse(user);
                if (userObj.loggedIn) {
                    // هدایت به پنل مناسب
                    const redirects = {
                        'guest': '/panel/guest',
                        'host': '/panel/host',
                        'security': '/panel/security',
                        'admin': '/panel/admin'
                    };
                    window.location.href = redirects[userObj.role];
                }
            }
        });
    </script>
</body>
</html>