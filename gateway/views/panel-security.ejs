<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>پنل حراست | GatePass360</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .panel-container {
            max-width: 1000px;
            margin: 80px auto 30px;
            padding: 0 20px;
        }

        .panel-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 3px 10px rgba(0,0,0,0.08);
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .user-icon {
            width: 50px;
            height: 50px;
            background: #e74c3c;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5rem;
        }

        .logout-btn {
            background: #e74c3c;
            color: white;
            border: none;
            padding: 8px 20px;
            border-radius: 5px;
            cursor: pointer;
        }

        .card {
            background: white;
            border-radius: 10px;
            box-shadow: 0 3px 10px rgba(0,0,0,0.08);
            padding: 25px;
            margin-bottom: 20px;
        }

        .card-title {
            color: #2c3e50;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #e74c3c;
        }

        .requests-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        .requests-table th {
            background: #f8f9fa;
            padding: 15px;
            text-align: right;
            border-bottom: 2px solid #eee;
            color: #2c3e50;
        }

        .requests-table td {
            padding: 12px 15px;
            border-bottom: 1px solid #eee;
        }

        .form-control {
            padding: 12px;
            border: 2px solid #ddd;
            border-radius: 5px;
            font-family: inherit;
            width: 100%;
            max-width: 300px;
        }

        .checkin-section {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
        }

        .checkin-box {
            flex: 1;
            min-width: 300px;
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
        }

        .checkin-box h3 {
            margin-bottom: 15px;
            color: #2c3e50;
        }

        .action-btn {
            padding: 8px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin: 0 2px;
            background: #3498db;
            color: white;
        }

        .action-btn.approve-btn {
            background: #27ae60;
        }

        .action-btn.approve-btn:hover {
            background: #219a52;
        }

        .action-btn.reject-btn {
            background: #e74c3c;
        }

        .action-btn.reject-btn:hover {
            background: #c0392b;
        }

        /* Status colors for different states */
        .status-pending-host { background: #fff3cd; color: #856404; }
        .status-pending-security { background: #cce5ff; color: #004085; }
        .status-approved { background: #d4edda; color: #155724; }
        .status-rejected-host { background: #f8d7da; color: #721c24; }
        .status-rejected-security { background: #f5c6cb; color: #721c24; }

        /* Rejection reason styling */
        .rejection-reason {
            color: #721c24;
            font-size: 0.9rem;
        }
        .rejection-reason i {
            margin-left: 5px;
        }

        .pass-code {
            font-family: monospace;
            font-size: 1.1rem;
            background: #e8f5e9;
            padding: 5px 10px;
            border-radius: 5px;
            color: #2e7d32;
        }
    </style>
</head>
<body>
    <!-- نوار بالا -->
    <nav class="navbar">
        <div class="container">
            <div class="logo">
                <i class="fas fa-door-closed"></i>
                <a href="/" style="text-decoration: none; color: inherit;">
                    GatePass<span class="highlight">360</span>
                </a>
            </div>
            <div class="nav-links">
                <a href="/">خانه</a>
            </div>
        </div>
    </nav>

    <!-- پنل حراست -->
    <div class="panel-container">
        <div class="panel-header">
            <div class="user-info">
                <div class="user-icon">
                    <i class="fas fa-shield-alt"></i>
                </div>
                <div>
                    <h3 id="userName">حراست</h3>
                    <p style="color: #666; font-size: 0.9rem;">🛡️ نقش: حراست</p>
                </div>
            </div>
            <button class="logout-btn" id="logoutBtn">
                <i class="fas fa-sign-out-alt"></i> خروج
            </button>
        </div>

        <!-- کارت درخواست‌های تأیید شده -->
        <div class="card">
            <h2 class="card-title">درخواست‌های در انتظار تأیید حراست</h2>
            <table class="requests-table">
                <thead>
                    <tr>
                        <th>مهمان</th>
                        <th>میزبان</th>
                        <th>تاریخ</th>
                        <th>هدف</th>
                        <th>عملیات</th>
                    </tr>
                </thead>
                <tbody id="approvedRequests">
                    <!-- پر می‌شود -->
                </tbody>
            </table>
        </div>

        <!-- کارت ثبت ورود/خروج -->
        <div class="card">
            <h2 class="card-title">ثبت ورود و خروج</h2>
            <div class="checkin-section">
                <div class="checkin-box">
                    <h3><i class="fas fa-sign-in-alt"></i> ورود مهمان</h3>
                    <form id="checkinForm">
                        <input type="text" placeholder="کد مجوز" class="form-control" style="margin-bottom: 10px;">
                        <button type="submit" class="btn-primary">ثبت ورود</button>
                    </form>
                </div>

                <div class="checkin-box">
                    <h3><i class="fas fa-sign-out-alt"></i> خروج مهمان</h3>
                    <form id="checkoutForm">
                        <input type="text" placeholder="کد مجوز" class="form-control" style="margin-bottom: 10px;">
                        <button type="submit" class="btn-primary">ثبت خروج</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="/javascripts/auth.js"></script>
    <script src="/javascripts/panel.js"></script>
    <script src="/javascripts/panel-security.js"></script>
    <script>
        // Initialize panel
        (async function() {
            const user = await initPanel(['security'], function(user) {
                // Display user info
                displayUserInfo(user);
            });
        })();
    </script>
</body>
</html>

